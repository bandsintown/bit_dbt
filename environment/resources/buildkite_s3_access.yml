Resources:
  DbtDagScriptsBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: ${self:custom.vars.airflow_environment_bucket_name}
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowAccountListDagsDbtPrefix
            Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
            Action:
              - s3:ListBucket
              - s3:GetBucketLocation
            Resource: !Sub "arn:aws:s3:::${self:custom.vars.airflow_environment_bucket_name}"
            Condition:
              StringLike:
                s3:prefix:
                  - dags/dependencies/dbt
                  - dags/dependencies/dbt/*
          - Sid: AllowAccountReadWriteDagsDbtObjects
            Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
            Action:
              - s3:GetObject
              - s3:PutObject
              - s3:DeleteObject
              - s3:AbortMultipartUpload
            Resource: !Sub "arn:aws:s3:::${self:custom.vars.airflow_environment_bucket_name}/dags/dependencies/dbt/*"

