version: 2

sources:
  - name: featured_events
    database: "{{ target.database }}"
    schema: featured_events
    tables:
      - name: featured_events_list
      - name: pixelactivities
      - name: email_impressions
      - name: ticketclicks
      - name: rsvps

